//define o caminho onde o sequelize deve buscar a configuração

const path = require('path');

module.export ={
    config: path.resolve(__dirname, 'src','config','databaseSQL.js'),
    'migrations-path': path.resolve(__dirname, 'src', database', 'sql', 'migrations'),
};

//yarn sequelize db:create    rodar no terminal para inicializar o db
//yarn sequelize migration:create --name=create-<nome da tabela>    rodar no terminal para criar tabela